class User {
  constructor(
    id,
    // firstName,
    // lastName,
    userName,
    email,
    password
    // avatar,
  ) {
    this.id = id
    // this.firstName = firstName
    // this.lastName = lastName
    this.userName = userName
    this.email = email
    this.password = password
    // this.avatar = avatar
  }
}

module.exports = User
